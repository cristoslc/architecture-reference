# Agent Specs

Agent Specs are behavior contracts — precise enough for an agent to create an implementation plan from, but concise enough to scan in a single pass. They define external behavior (inputs, outputs, preconditions, constraints) and are scoped to something that can be shipped and validated independently.

See [list-spec.md](list-spec.md) for the lifecycle index.
