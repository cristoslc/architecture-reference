project_name: AutoGPT
scope: platform
use_type: production
source: Discovered
source_url: https://github.com/Significant-Gravitas/AutoGPT
project_url: https://github.com/Significant-Gravitas/AutoGPT
evidence_type: automated-discovery
discovered_at: '2026-03-06T06:05:36Z'
discovered_by: discover-architecture
discovery_version: 1.0.0
domain: AI/ML
language: Python
languages:
- Python
- TypeScript
- JavaScript
architecture_styles:
- Pipeline
- Microservices
classification_status: classified
review_notes: 'The AutoGPT repository has evolved from a single-script experiment
  into a sophisticated distributed system. The architecture is now split into two
  main sections: "Classic" (legacy agents) and the "Platform" (the modern distributed
  infrastructure).


  ### 1. Primary Style: Microservices

  The `autogpt_platform` is structured as a set of independently executable services.
  Evidence from `autogpt_platform/backend/pyproject.toml` shows defined entry points
  for distinct operational roles that correspond to a microservices topology:

  - **API/Rest Service**: `backend.rest:main` (External interface)

  - **Executor**: `backend.executor:main` (Computational/Agent worker)

  - **Scheduler**: `backend.scheduler:main` (Temporal coordination)

  - **Notification**: `backend.notification:main` (Side-effect management)

  - **Websocket Service**: `backend.ws:main` (Real-time updates)'
review_required: false
classification_method: deep-analysis-tooluse
classification_confidence: 0.9
classification_model: openrouter/z-ai/glm-5
classification_date: '2026-03-08T19:21:46Z'
classification_reasoning: "## Analysis\n\n### Primary Architecture: Pipeline (Pipe-and-Filter)\n\
  \nThe AutoGPT Platform is built around a **Block-based pipeline system** where:\n\
  \n1. **Blocks are the fundamental processing units** - There are **275+ blocks**\
  \ in `autogpt_platform/backend/backend/blocks/`, each defining:\n   - Input schema\
  \ (`BlockSchemaInput`) \n   - Output schema (`BlockSchemaOutput`)\n   - An async\
  \ `run()` method that yields output tuples\n\n2. **Graphs (Agents) are composed\
  \ of connected blocks** - From `backend/data/graph.py`:\n   - `Node` instances reference\
  \ a `block_id` \n   - `Link` instances connect source blocks to sink blocks via\
  \ `source_id`, `sink_id`, `source_name`, `sink_name`\n   - Data flows through the\
  \ graph from block to block\n\n3. **Execution follows pipeline semantics** - The\
  \ `ExecutionManager` in `backend/executor/manager.py` orchestrates graph execution\
  \ by:\n   - Queueing execution via RabbitMQ\n   - Executing blocks in dependency\
  \ order\n   - Passing output from one block as input to connected blocks\n\n4. **Block\
  \ categories define processing stages** - From `_base.py`, blocks are categorized\
  \ as AI, TEXT, SEARCH, LOGIC, COMMUNICATION, etc. - each performing a specific transformation.\n\
  \n5. **Dynamic block discovery** - From `blocks/__init__.py`:\n```python\ndef load_all_blocks()\
  \ -> dict[str, type[\"AnyBlockSchema\"]]:\n    # Dynamically load all modules under\
  \ backend.blocks\n    for f in current_dir.rglob(\"*.py\"):\n        importlib.import_module(f\"\
  .{module}\", package=__name__)\n    # Load all Block instances from the available\
  \ modules\n    for block_cls in _all_subclasses(Block):\n        available_blocks[block.id]\
  \ = block_cls\n```\n\n### Secondary Architecture: Microservices\n\nThe platform\
  \ is deployed as **multiple independently deployable services** (from `docker-compose.platform.yml`):\n\
  \n- **rest_server** - FastAPI REST API (port 8006)\n- **executor** - Graph execution\
  \ engine (port 8002)  \n- **copilot_executor** - AI copilot processing (port 8008)\n\
  - **websocket_server** - Real-time client updates\n- **database_manager** - Database\
  \ operations\n- **scheduler_server** - Scheduled job execution\n- **notification_server**\
  \ - Notification handling\n- **frontend** - Next.js web application\n\n**Asynchronous\
  \ communication via RabbitMQ** - Services communicate through message queues defined\
  \ in `backend/data/rabbitmq.py`:\n```python\nclass RabbitMQBase(ABC):\n    \"\"\"\
  Base class for RabbitMQ connections with shared configuration\"\"\"\n```\n\n**Shared\
  \ infrastructure**: PostgreSQL (via Supabase), Redis for caching, RabbitMQ for messaging.\n\
  \n### What This Is NOT\n\n- **NOT Layered** - While there are API/executor/data\
  \ components, the core abstraction is graph-based flow, not horizontal layering\n\
  - **NOT Modular Monolith** - Multiple independent Docker services with separate\
  \ deployment units\n- **NOT CQRS** - No explicit read/write model separation\n-\
  \ **NOT DDD** - No bounded contexts or aggregate patterns\n- **NOT Hexagonal Architecture**\
  \ - While integrations exist, the core isn't organized around ports-and-adapters\
  \ isolation\n\n### Supporting Evidence\n\n**Block-based composition** - Example\
  \ from `blocks/basic.py`:\n```python\nclass FileStoreBlock(Block):\n    async def\
  \ run(self, input_data: Input, ...) -> BlockOutput:\n        yield \"file_out\"\
  , await store_media_file(...)\n```\n\n**Flow control blocks** - `blocks/branching.py`\
  \ shows conditional routing:\n```python\nclass ConditionBlock(Block):\n    # Routes\
  \ data based on comparison operators\n    # Outputs to yes_output or no_output based\
  \ on condition\n```\n\n**Graph execution** - From `executor/manager.py`:\n```python\n\
  from backend.data.graph import Link, Node\nfrom backend.data.execution import GraphExecution,\
  \ NodeExecutionEntry\n```\n\n**275+ block files** provide extensive processing capabilities:\n\
  ```\n$ find autogpt_platform/backend/backend/blocks -name \"*.py\" -type f | wc\
  \ -l\n275\n```\n\n### Conclusion\n\nThe AutoGPT Platform implements a **Pipeline\
  \ (Pipe-and-Filter)** architecture where:\n- **Filters** = Blocks (processing units\
  \ with defined input/output schemas)\n- **Pipes** = Links (connections between block\
  \ outputs and inputs)\n- **Graphs** = Compositions of interconnected blocks forming\
  \ complete workflows/agents\n\nThis is enhanced by a **Microservices** deployment\
  \ topology where services communicate asynchronously via RabbitMQ, enabling independent\
  \ scaling and deployment of the executor, API, scheduler, and other components."
